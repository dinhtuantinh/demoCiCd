// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
  /* Other config options here */
  output: 'export',
  images: {
    unoptimized: true,
  }
};

module.exports = nextConfig;
